@if(!empty($siteFaviconUrl))
<link rel="icon" href="{{ $siteFaviconUrl }}" type="image/png" sizes="32x32">
<link rel="icon" href="{{ $siteFaviconUrl }}" type="image/png" sizes="192x192">
<link rel="apple-touch-icon" href="{{ $siteFaviconUrl }}">
@endif
