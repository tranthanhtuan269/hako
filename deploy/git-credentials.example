# Copy to /etc/hakoreview/git-credentials (chmod 600) for cron git pull.
# Create a fine-grained GitHub PAT with Contents: Read on repo tranthanhtuan269/hako.
#
#   sudo cp deploy/git-credentials.example /etc/hakoreview/git-credentials
#   sudo chmod 600 /etc/hakoreview/git-credentials
#   sudo nano /etc/hakoreview/git-credentials
#
GITHUB_USER=tranthanhtuan269
GITHUB_TOKEN=ghp_your_token_here
